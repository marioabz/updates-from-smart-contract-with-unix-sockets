# Sending Reserves updates through Unix Sockets

## Running scripts
To create socket where data is going to be sent to: `
    - `cd reader/`
    - `go run reader.go`

To write data to the socket:
    - `go run main.go`
